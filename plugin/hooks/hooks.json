{
  "description": "Tell Claude when this session runs under a different claude-switcher profile than its directory resolves to.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v claude-switcher >/dev/null 2>&1; then claude-switcher hook session-start; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
