{
  "name": "claude-account-switcher-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cp ../llms.txt public/llms.txt && vitepress dev .",
    "build": "cp ../llms.txt public/llms.txt && vitepress build .",
    "preview": "vitepress preview .",
    "deploy": "npm run build && wrangler deploy -c ../deploy/wrangler.jsonc"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "^4"
  }
}
